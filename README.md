# popstock

A lightweight package for quick evaluation of the stochastic GW energy density `Omega_GW` emitted by a population of binary black holes and/or neutron stars. This package relies on importance sampling methods and is set up to optimize computations using both CPUs and GPUs.
